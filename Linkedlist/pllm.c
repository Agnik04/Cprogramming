#include "pll.h"

int main()
{
    Node* h1 = NULL, *h2 = NULL, *temp;

    int i, ch, value, count = 1, position;

    while(1)
    {
        printf("1: Insert polynomial values");
        printf("2: To add the polynomial.\n");
        printf("3 To Exit.\n\n");

        printf("Enter your choice : ");
        scanf("%d",&ch);

    return 0;
}